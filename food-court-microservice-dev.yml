server:
  port: 8091

spring:
  application:
    name: food-court-microservice
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: off

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8099/eureka