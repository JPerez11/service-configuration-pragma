server:
  port: 8090
  
spring:
  application:
    name: user-microservice
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: off

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8099/eureka